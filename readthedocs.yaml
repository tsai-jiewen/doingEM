# Optionally declare the Python requirements required to build your docs
python:
   install:
     - method: pip
       path: .
       extra_requirements:
         - mkdocs-jupyter
   
# Build all formats
formats: all